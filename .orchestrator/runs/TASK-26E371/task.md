# Task: TASK-26E371

## Title: # Task: TASK-9FB44F  ## Title: chcialbym moc miec projekty ktore moge otwierac na roznych urzadzenia

# Task: TASK-9FB44F  ## Title: chcialbym moc miec projekty ktore moge otwierac na roznych urzadzeniach, jesli wprowadze zmiane na P  chcialbym moc miec projekty ktore moge otwierac na roznych urzadzeniach, jesli wprowadze zmiane na PC, po zalogowaniu na telefon moge otworzyc ten sam projekt (bez koniecznosci uploadowania tego projektu).  Zrob Najlepsze podejście: server-side project storage z auth. - przy czym poki co chce uniknac zewnetrznych baz (postgres) - poki co mam tylko uzytkownikow wiec chcialbym ich trzymac poki co lokalnie (moze byc nawet w pliku, ale zeby musieli sie logowac - bez mozliwosci wejscia bez zalogowania)  1. Autentykacja - Schema users już istnieje w schema.ts - Dodaj login/register z sesją (np. express-session + bcrypt) lub użyj zewnętrznego provider'a (Google OAuth przez better-auth/lucia) - Na telefonie logujesz się tym samym kontem  2. Projects API (backend) - Dodaj tabelę projects do Drizzle schema (id, userId, name, data jsonb, updatedAt) - Endpoints: GET /api/projects, POST /api/projects, PUT /api/projects/:id, DELETE /api/projects/:id - data to cały EditorState zserializowany do JSON (już to robisz przy eksporcie ZIP)  3. Auto-save (frontend) - Przy każdej zmianie stanu — debounce + PUT /api/projects/:id - Lista projektów pobierana z serwera zamiast localStorage - PDF-y jako osobne pliki (upload do serwera, przechowuj URL, nie base64 w JSON) — inaczej projekty z dużymi plikami będą wolne  Na ten moment samych uzytkownikow przechowuj lokalnie w pliku (bedzie max 10 uzytkownikow) ; Pozwrol mi dzielic projekt z innym uzytkownikiem (zeby byl dla niego widoczny)  Reszta EditorState (warstwy, obiekty, metadane, ustawienia) idzie bezpośrednio do kolumny state jsonb.                    ---                                                                                                                                                                                                                                                                                                       Przepływ danych  Otwierasz projekt na PC: GET /api/projects          → lista z baza                                                                                                                                                                                                                                                         GET /api/projects/:id      → state JSON z baza                                                                                                                                                                                                                                                    GET /api/files/:id/...     → PDF/ikony/zdjęcia z dysku  Zapisujesz zmianę: PUT /api/projects/:id      → state JSON → baza danych (debounce ~2s)                                      POST /api/files/:id/...    → nowe pliki → dysk (tylko przy dodaniu)  Otwierasz na telefonie:  Logujesz się → ten sam user_id → te same projekty   
