# Task Completion Workflow
1. Run `npm run check` to ensure type safety.
2. Verify changes manually in the UI (since there are no automated tests visible in package.json).
3. Ensure documentation is updated if new core logic is added.
