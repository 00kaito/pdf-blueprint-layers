## Priority 5 — Custom Hooks: Extract Business Logic from Components

### 5.1 `hooks/useExport.ts`
✅ **ZREALIZOWANE**: Cała logika eksportu PDF oraz projektu została przeniesiona do dedykowanego hooka.
- Dodano poprawkę [Bug A]: bezpieczny fallback `(layer.opacity ?? 1)` zapobiegający błędom `NaN` przy ładowaniu starych plików.
- Przywrócono logikę zawijania tekstu (word-wrap) dla obiektów typu `text`.
- Rozszerzono eksport projektu o `customIcons`, `exportSettings` oraz `autoNumbering`.

### 5.2 `hooks/useObjectCreation.ts`
✅ **ZREALIZOWANE**: Logika tworzenia obiektów i obliczania ich pozycji początkowej (środka widoku) została wydzielona.

### 5.3 `hooks/useDrawing.ts`
✅ **ZREALIZOWANE**: Zarządzanie stanem rysowania i obsługa zdarzeń myszy przeniesiona do hooka.
- Dodano poprawkę [Bug B]: zastąpiono sztywne wartości `600, 800` stałymi `CANVAS_BASE_WIDTH` i `CANVAS_BASE_HEIGHT`.

---

#### STATUS: DONE
Wszystkie błędy naprawione, a funkcjonalność eksportu/importu projektu została zsynchronizowana z pełnym stanem aplikacji.
